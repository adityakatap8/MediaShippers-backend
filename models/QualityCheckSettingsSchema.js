import mongoose from "mongoose";


const QualityCheckSettings = new mongoose.schema({
    
})




export default mongoose.models.CodecSettings || mongoose.model("QualityCheckSettings", QualityCheckSettings);